# 💰 Партнерская программа и PRO-подписка за Stars

## 📋 Обзор системы

Telegram предоставляет партнерскую программу для ботов и mini apps, позволяя:
1. **Вам** - монетизировать приложение через продажу PRO-подписки за Stars
2. **Партнерам** - получать % от покупок пользователей, которых они привели

## 🎯 Как это работает

### Схема монетизации:
```
Пользователь → Партнерская ссылка → Регистрация
    ↓
Покупка PRO за 500 Stars
    ↓
Вы получаете: 350-450 Stars (70-90%)
Партнер получает: 50-150 Stars (10-30%)
```

### Ваша выгода:
- **Пассивный доход** от каждого платного пользователя
- **Бесплатный маркетинг** через партнеров
- **Масштабирование** без затрат на рекламу

### Выгода партнера:
- **Процент от покупок** пользователей навсегда (или на установленный период)
- **Автоматические выплаты** на Star-баланс
- **Пассивный доход** от аудитории

## 💳 Шаг 1: Создание PRO-подписки за Stars

### Что входит в PRO:
- ✅ Безлимитные сообщения
- ✅ Приоритет в поиске
- ✅ Расширенные фильтры
- ✅ Просмотр кто лайкнул профиль
- ✅ Отсутствие рекламы
- ✅ Эксклюзивный бейдж PRO

### Цены (рекомендуемые):
- **1 месяц PRO**: 250 Stars (~$5)
- **3 месяца PRO**: 600 Stars (~$12, скидка 20%)
- **6 месяцев PRO**: 1000 Stars (~$20, скидка 33%)
- **1 год PRO**: 1800 Stars (~$36, скидка 40%)

*1 Star ≈ $0.02 (2 цента)*

## 🔧 Шаг 2: Техническая реализация

### 2.1. Добавить команду /premium в бота

```python
async def premium_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Команда /premium - покупка PRO подписки"""
    user = update.effective_user
    
    premium_text = (
        "⭐ <b>Anonimka PRO</b>\n\n"
        "Получи максимум от анонимных знакомств!\n\n"
        "<b>Что входит в PRO:</b>\n"
        "✅ Безлимитные сообщения\n"
        "✅ Приоритет в поиске\n"
        "✅ Расширенные фильтры\n"
        "✅ Просмотр кто лайкнул\n"
        "✅ Без рекламы\n"
        "✅ Эксклюзивный бейдж\n\n"
        "<b>Выбери тариф:</b>"
    )
    
    keyboard = [
        [InlineKeyboardButton("⭐ 1 месяц - 250 Stars", callback_data="buy_premium_1")],
        [InlineKeyboardButton("⭐ 3 месяца - 600 Stars (-20%)", callback_data="buy_premium_3")],
        [InlineKeyboardButton("⭐ 6 месяцев - 1000 Stars (-33%)", callback_data="buy_premium_6")],
        [InlineKeyboardButton("⭐ 1 год - 1800 Stars (-40%)", callback_data="buy_premium_12")],
        [InlineKeyboardButton("❓ Как купить Stars", url="https://t.me/PremiumBot")]
    ]
    
    await update.message.reply_text(
        premium_text,
        parse_mode='HTML',
        reply_markup=InlineKeyboardMarkup(keyboard)
    )
```

### 2.2. Создать invoice для оплаты Stars

```python
from telegram import LabeledPrice

async def buy_premium_callback(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик покупки PRO"""
    query = update.callback_query
    await query.answer()
    
    # Определяем тариф
    plans = {
        "buy_premium_1": {"months": 1, "price": 250, "title": "1 месяц PRO"},
        "buy_premium_3": {"months": 3, "price": 600, "title": "3 месяца PRO"},
        "buy_premium_6": {"months": 6, "price": 1000, "title": "6 месяцев PRO"},
        "buy_premium_12": {"months": 12, "price": 1800, "title": "1 год PRO"}
    }
    
    plan = plans.get(query.data)
    if not plan:
        return
    
    # Создаем invoice для оплаты Stars
    title = f"⭐ {plan['title']}"
    description = (
        f"Подписка Anonimka PRO на {plan['months']} мес.\n"
        "Безлимитные сообщения, приоритет в поиске и эксклюзивные функции!"
    )
    
    prices = [LabeledPrice(label=plan['title'], amount=plan['price'])]
    
    # Payload - данные для идентификации платежа
    payload = f"premium_{plan['months']}_{query.from_user.id}"
    
    await context.bot.send_invoice(
        chat_id=query.message.chat_id,
        title=title,
        description=description,
        payload=payload,
        provider_token="",  # Пустая строка для Stars
        currency="XTR",  # Валюта Stars
        prices=prices,
        photo_url="https://anonimka.kz/premium-badge.jpg",  # Картинка PRO
        photo_width=512,
        photo_height=512
    )
```

### 2.3. Обработать успешный платеж

```python
async def successful_payment_callback(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик успешного платежа Stars"""
    payment = update.message.successful_payment
    user_id = update.effective_user.id
    
    # Парсим payload чтобы узнать что купили
    payload_parts = payment.invoice_payload.split('_')
    months = int(payload_parts[1])
    
    # Активируем PRO через API
    try:
        async with aiohttp.ClientSession() as session:
            async with session.post(
                f'{API_BASE_URL}/api/premium/activate',
                json={
                    'telegram_id': user_id,
                    'months': months,
                    'transaction_id': payment.telegram_payment_charge_id
                }
            ) as resp:
                if resp.status == 200:
                    await update.message.reply_text(
                        f'🎉 <b>Поздравляем!</b>\n\n'
                        f'PRO подписка активирована на {months} мес.!\n\n'
                        f'✨ Все функции уже доступны в приложении.\n'
                        f'Спасибо что с нами! ❤️',
                        parse_mode='HTML'
                    )
                    logger.info(f'✅ PRO активирован для {user_id} на {months} мес.')
                else:
                    await update.message.reply_text(
                        '❌ Ошибка активации PRO. Обратитесь в поддержку.'
                    )
    except Exception as e:
        logger.error(f'Ошибка активации PRO: {e}')
        await update.message.reply_text(
            '❌ Ошибка активации. Напишите в поддержку: @Vorobey_444'
        )

# Добавить в main():
application.add_handler(CommandHandler("premium", premium_command))
application.add_handler(CallbackQueryHandler(buy_premium_callback, pattern="^buy_premium_"))
application.add_handler(MessageHandler(filters.SUCCESSFUL_PAYMENT, successful_payment_callback))
```

## 🤝 Шаг 3: Создание партнерской программы

### 3.1. Настройка через BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Выберите `/mybots` → ваш бот → **Affiliate Program**
3. Нажмите **Create Affiliate Program**
4. Настройте параметры:

**Рекомендуемые настройки:**
```
Commission Rate: 20-30%
Commission Period: 365 days (1 год) или Lifetime (навсегда)
```

### 3.2. Как партнеры получают ссылки

Пользователи могут получить партнерскую ссылку:
1. Открыв профиль вашего бота
2. Нажав **"Earn with this bot"** (появится автоматически)
3. Получив уникальную ссылку типа:
   ```
   https://t.me/anonimka_kz_bot?start=ref_ABC123
   ```

### 3.3. Добавить кнопку "Стать партнером" в бота

```python
async def affiliate_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Команда /affiliate - информация о партнерской программе"""
    affiliate_text = (
        "🤝 <b>Партнерская программа Anonimka</b>\n\n"
        "Зарабатывай на рекомендациях!\n\n"
        "<b>Как это работает:</b>\n"
        "1️⃣ Получи свою партнерскую ссылку\n"
        "2️⃣ Поделись с друзьями/подписчиками\n"
        "3️⃣ Получай 25% от их покупок PRO\n\n"
        "<b>Условия:</b>\n"
        "💰 25% комиссия от всех покупок\n"
        "⏰ Навсегда (lifetime)\n"
        "💳 Выплаты в Telegram Stars\n"
        "📊 Прозрачная статистика\n\n"
        "<b>Пример:</b>\n"
        "Друг купил PRO за 1000 Stars\n"
        "Ты получаешь: 250 Stars (~$5)\n\n"
        "Начни зарабатывать прямо сейчас! 👇"
    )
    
    keyboard = [
        [InlineKeyboardButton("🔗 Получить партнерскую ссылку", 
                             url=f"https://t.me/{context.bot.username}?startattach=ref")],
        [InlineKeyboardButton("📊 Статистика заработка", callback_data="affiliate_stats")],
        [InlineKeyboardButton("❓ Как это работает", callback_data="affiliate_help")]
    ]
    
    await update.message.reply_text(
        affiliate_text,
        parse_mode='HTML',
        reply_markup=InlineKeyboardMarkup(keyboard)
    )
```

## 💡 Шаг 4: Продвижение партнерской программы

### 4.1. Добавить в главное меню бота

Добавьте кнопку "💰 Заработать" в `/start` и `/menu`:

```python
[InlineKeyboardButton("💰 Стать партнером", callback_data="affiliate_info")]
```

### 4.2. Уведомления пользователям

После покупки PRO показывайте:
```
✅ PRO активирован!

💡 Знаешь друзей которым понравится Anonimka?
Стань партнером и зарабатывай 25% с их покупок!

/affiliate - узнать подробнее
```

### 4.3. Пост в канале

```
🤝 ПАРТНЕРСКАЯ ПРОГРАММА ANONIMKA

Зарабатывай рекомендуя друзьям!

💰 Условия:
• 25% с каждой покупки PRO
• Навсегда (lifetime tracking)
• Автоматические выплаты в Stars
• Без лимитов

📊 Пример заработка:
10 рефералов × 500 Stars = 1250 Stars твоих (~$25)

Начни зарабатывать: @anonimka_kz_bot → /affiliate
```

## 📊 Шаг 5: Аналитика и отчетность

### API эндпоинт для статистики партнера

```javascript
// /api/affiliate/stats
export default async function handler(req, res) {
  const { telegram_id } = req.query;
  
  // Получаем статистику из БД
  const stats = await db.query(`
    SELECT 
      COUNT(DISTINCT referred_user_id) as total_referrals,
      SUM(commission_amount) as total_earned,
      COUNT(CASE WHEN purchased = true THEN 1 END) as paid_referrals
    FROM referrals
    WHERE referrer_id = $1
  `, [telegram_id]);
  
  res.json({
    referrals: stats.total_referrals,
    earned: stats.total_earned,
    paid: stats.paid_referrals,
    conversion: (stats.paid_referrals / stats.total_referrals * 100).toFixed(1)
  });
}
```

## 💰 Прогноз доходов

### Пример расчета:

**Ваши показатели:**
- Пользователей: 1000
- Конверсия в PRO: 5% = 50 покупок/месяц
- Средний чек: 500 Stars
- Доход: 50 × 500 × 0.75 = 18,750 Stars/месяц (~$375)

**С партнерской программой:**
- Партнеров: 20
- Рефералов от каждого: 10
- Покупки: 200 × 5% = 10 покупок/месяц
- Доход: 10 × 500 × 0.75 = 3,750 Stars доп. (~$75)
- **Итого: $450/месяц вместо $375** (+20% роста)

### Масштабирование:
- **100 партнеров** → +$375/мес
- **500 партнеров** → +$1,875/мес
- **1000 партнеров** → +$3,750/мес

## ⚠️ Важные моменты

### 1. Налоги и комиссии
- **Telegram берет 30%** от платежа (Apple/Google комиссия)
- **Партнер получает** от вашей части (70%)
- **Вы устанавливаете** % партнера (рекомендую 20-30%)

### Реальная математика:
```
Пользователь платит: 500 Stars
Telegram забирает: 150 Stars (30%)
Вам остается: 350 Stars

Из ваших 350 Stars:
- Партнеру (25%): 87.5 Stars
- Вам остается: 262.5 Stars
```

### 2. Период комиссии
- **30-90 дней**: короткий срок, меньше мотивации партнеров
- **180-365 дней**: хороший баланс
- **Lifetime**: максимальная мотивация, но больше расходов

**Рекомендация:** 365 дней (1 год) - оптимальный баланс

### 3. Нельзя изменить после запуска
- ❌ Нельзя уменьшить % комиссии
- ❌ Нельзя сократить период
- ✅ Можно увеличить % комиссии
- ✅ Можно продлить период
- ✅ Можно полностью завершить программу

### 4. Withdrawal ограничения
- Stars от партнерки **нельзя использовать 21 день**
- Защита от фрода и возвратов
- После 21 дня - полная свобода использования

## 🚀 План запуска (Next Steps)

### Неделя 1: Разработка
- [ ] Добавить команду `/premium` в бота
- [ ] Реализовать покупку через Stars invoice
- [ ] Добавить обработку successful_payment
- [ ] Создать API endpoint для активации PRO
- [ ] Обновить БД: добавить поля premium_until, transaction_id

### Неделя 2: Настройка партнерской программы
- [ ] Создать программу через BotFather (25%, 365 дней)
- [ ] Добавить команду `/affiliate`
- [ ] Добавить кнопку "Стать партнером" в меню
- [ ] Создать справку по партнерской программе

### Неделя 3: Тестирование
- [ ] Тестовая покупка PRO за Stars
- [ ] Проверка активации подписки
- [ ] Тестирование партнерской ссылки
- [ ] Проверка начисления комиссии

### Неделя 4: Запуск
- [ ] Анонс PRO подписки в канале
- [ ] Анонс партнерской программы
- [ ] Email рассылка существующим пользователям
- [ ] Пост в Instagram/Stories

## 📚 Полезные ссылки

- **BotFather**: https://t.me/BotFather
- **Telegram Stars Docs**: https://core.telegram.org/bots/payments
- **Affiliate Program Terms**: https://telegram.org/tos/affiliate-program
- **PremiumBot** (купить Stars): https://t.me/PremiumBot

---

**💡 Главное преимущество:** Партнерская программа создает **вирусный эффект** - каждый довольный пользователь становится вашим маркетологом, получая долю прибыли. Это **win-win** для всех!

**🎯 Прогноз:** При 1000 активных пользователей и 5% конверсии в PRO, с партнерской программой вы можете вырасти до **2000-3000 пользователей за 3 месяца** за счет органического роста через рефералов.
