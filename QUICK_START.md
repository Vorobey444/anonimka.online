# 🚀 Быстрый старт Neon бота

## За 3 шага к работающему боту!

### 1️⃣ Установите зависимости

```bash
install_neon_bot.bat
```

Или вручную:
```bash
pip install python-telegram-bot aiohttp python-dotenv
```

### 2️⃣ Настройте переменные окружения

Создайте файл `.env` из `.env.example`:
```bash
copy .env.example .env
```

Откройте `.env` и заполните:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
VERCEL_API_URL=https://anonimka.kz
```

### 3️⃣ Запустите бота

```bash
start_bot.bat
```

Выберите опцию **[3] 🔥 НОВЫЙ: Neon бот**

---

## ✅ Готово!

Бот запущен и работает с Neon PostgreSQL!

### Проверьте работу:

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите "🚀 Открыть приложение"
4. Создайте чат в WebApp
5. Отправьте `/my_chats` в боте
6. Выберите чат и отправьте сообщение
7. Проверьте в WebApp - сообщение должно появиться!

---

## 🆘 Проблемы?

### Ошибка: "TELEGRAM_BOT_TOKEN не установлен"
- Проверьте что файл `.env` существует
- Проверьте что токен правильно скопирован

### Бот не отвечает
- Проверьте что выполнены миграции в Neon (см. NEON_MIGRATIONS_GUIDE.md)
- Проверьте логи в консоли

### Сообщения не синхронизируются
- Убедитесь что WebApp задеплоен на Vercel
- Проверьте что `VERCEL_API_URL` правильный

---

## 📚 Дополнительно

- Полная инструкция: `BOT_NEON_GUIDE.md`
- Миграции БД: `NEON_MIGRATIONS_GUIDE.md` (в папке anonimka-nextjs)
- Исходный код: `bot_neon.py`
