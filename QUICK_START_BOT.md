# 🚀 Быстрый старт - Бот активности

## За 5 минут до запуска

### Шаг 1: Создайте таблицу в Neon
```sql
-- Откройте Neon Dashboard → SQL Editor
-- Скопируйте и выполните:

CREATE TABLE IF NOT EXISTS world_chat_messages (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255) NOT NULL,
    message TEXT NOT NULL,
    is_bot BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_world_chat_created_at ON world_chat_messages(created_at DESC);
```

### Шаг 2: Проверьте .env файл
```env
TELEGRAM_BOT_TOKEN=your_token_here
VERCEL_API_URL=https://anonimka.kz
DATABASE_URL=postgresql://...
```

### Шаг 3: Деплой API
Убедитесь что файл `/api/world-chat.js` задеплоен на Vercel.

### Шаг 4: Тест
```bash
test_bot.bat
```

Если видите ✅ — всё работает!

### Шаг 5: Запуск
**Для начала:**
```bash
start_activity_bot.bat
```

**Для production:**
```bash
start_smart_bot.bat
```

---

## Что произойдёт?

```
🤖 Бот запустится
   ↓
⏱️ Каждые 30 секунд проверяет чат
   ↓
👤 Если пользователь написал "Привет"
   ↓
🤖 Бот ответит "Привет! Как дела?"
   ↓
💬 Каждые 2-10 минут отправляет случайное сообщение
   ↓
🔄 Цикл повторяется
```

---

## Ожидаемый результат

### В чате вы увидите:
```
😎 Алекс: Эй, кто-нибудь тут?
🌸 Мария: Всем привет! Как настроение? 😊
🎮 Дима: Как дела?
✨ Катя: Доброе утро всем! ☀️✨
```

### Когда пользователь пишет:
```
👤 Пользователь: Привет!
   ↓ (2-5 сек)
🌸 Мария: Привет-привет! 🤗
```

---

## Проверка работы

1. **Откройте терминал** где запущен бот
2. **Ищите логи:**
   ```
   ✅ Отправлено: [bot_alex] Привет всем!
   📩 Новое сообщение от user123
   ```
3. **Проверьте чат** в приложении
4. **Напишите "Привет"** — бот должен ответить

---

## Если не работает

### ❌ Ошибка подключения к API
```
Проверьте:
- VERCEL_API_URL в .env
- API /api/world-chat доступен
- Нет CORS ошибок
```

### ❌ Ошибка базы данных
```
Проверьте:
- Таблица world_chat_messages создана
- DATABASE_URL правильный
- У пользователя БД есть права
```

### ❌ Бот не отвечает
```
Проверьте:
- Бот запущен (логи идут?)
- Сообщения сохраняются в БД
- isBot флаг работает
```

---

## Настройка под свою аудиторию

### Добавьте локальных персонажей:
```python
{
    "id": "bot_arman",
    "name": "Арман",
    "messages": [
        "Сәлем!",
        "Қалыңдар қалай?",
        "Алматыдан кім бар?",
    ]
}
```

### Добавьте местный сленг:
```python
"сәлем|salem|салам": {
    "type": "greeting",
    "responses": [
        "Сәлеметсіз бе!",
        "Сәлем! Жақсы ма?",
    ]
}
```

---

## Мониторинг

### Проверка логов в реальном времени:
```bash
# Windows
Get-Content -Path "bot.log" -Wait

# Linux
tail -f bot.log
```

### Проверка сообщений в БД:
```sql
SELECT * FROM world_chat_messages 
ORDER BY created_at DESC 
LIMIT 20;
```

---

## Production Setup

### Запуск на сервере (PM2):
```bash
pm2 start chat_activity_bot_advanced.py --name "smart-bot"
pm2 logs smart-bot
pm2 monit
```

### Автозапуск:
```bash
pm2 startup
pm2 save
```

---

## Полезные команды

```bash
# Остановить бота
Ctrl + C

# Перезапустить
pm2 restart smart-bot

# Посмотреть статус
pm2 status

# Очистить логи
pm2 flush

# Удалить из PM2
pm2 delete smart-bot
```

---

## FAQ

**Q: Сколько сообщений в день?**
A: ~50-100 (1 каждые 15-30 минут + ответы пользователям)

**Q: Можно изменить частоту?**
A: Да, измените `MESSAGE_INTERVAL_MIN/MAX`

**Q: Боты видны как боты?**
A: Да, есть флаг `isBot`, но в UI можно скрыть

**Q: Нужно ли останавливать на ночь?**
A: Нет, но можно снизить частоту

---

## Что дальше?

1. ✅ Запустите бота
2. ✅ Понаблюдайте 1-2 часа
3. ✅ Настройте персонажей
4. ✅ Добавьте больше фраз
5. ✅ Деплойте на сервер
6. ✅ Настройте мониторинг
7. ✅ Наслаждайтесь активным чатом! 🎉

---

**Готово! Теперь у вас есть живой чат! 🚀**
