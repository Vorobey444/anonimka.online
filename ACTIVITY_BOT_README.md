# 🤖 Бот активности для мир-чата

## Описание

Автоматический бот для создания активности в общем чате приложения. Создает видимость живого общения и отвечает реальным пользователям.

## Возможности

✅ **5 виртуальных персонажей** с разными стилями общения:
- Алекс - непринужденный стиль
- Мария - дружелюбный стиль  
- Дима - краткий стиль
- Катя - много эмодзи
- Артём - задает вопросы

✅ **Автоматические сообщения**
- Отправка случайных сообщений каждые 2-10 минут
- Разнообразные темы: музыка, фильмы, путешествия, технологии

✅ **Умные ответы**
- Автоматически отвечает на приветствия
- Реагирует на ключевые слова
- Поддерживает диалог с реальными пользователями

✅ **Естественное поведение**
- Случайные интервалы между сообщениями
- Имитация набора текста (задержки)
- Разнообразие фраз

## Установка

### 1. Создайте таблицу в Neon Database

Откройте Neon Dashboard и выполните SQL из файла `neon_world_chat.sql`:

```sql
CREATE TABLE IF NOT EXISTS world_chat_messages (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255) NOT NULL,
    message TEXT NOT NULL,
    is_bot BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 2. Проверьте .env файл

Убедитесь что в `.env` файле есть:

```env
TELEGRAM_BOT_TOKEN=your_bot_token
VERCEL_API_URL=https://anonimka.kz
DATABASE_URL=your_neon_database_url
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

Нужные пакеты:
- `aiohttp` - для HTTP запросов
- `python-dotenv` - для переменных окружения
- `asyncio` - для асинхронной работы

## Запуск

### Windows

Запустите `start_activity_bot.bat`:

```bash
start_activity_bot.bat
```

### Linux/Mac

```bash
python chat_activity_bot.py
```

## Настройка

Откройте `chat_activity_bot.py` и измените параметры:

```python
# Интервал проверки новых сообщений (секунды)
CHAT_CHECK_INTERVAL = 30

# Минимальный интервал между сообщениями ботов (секунды)
MESSAGE_INTERVAL_MIN = 120

# Максимальный интервал между сообщениями ботов (секунды)
MESSAGE_INTERVAL_MAX = 600
```

### Добавление новых персонажей

```python
VIRTUAL_PERSONAS.append({
    "id": "bot_ivan",
    "name": "Иван",
    "style": "casual",
    "topics": ["спорт", "авто"],
    "messages": [
        "Привет всем!",
        "Кто за футбол?",
        # ... добавьте свои фразы
    ]
})
```

### Добавление новых ответов

```python
RESPONSE_TEMPLATES["новая тема|ключевое слово"] = [
    "Ответ 1",
    "Ответ 2",
    "Ответ 3",
]
```

## Как это работает

```
┌─────────────────────────────────────────┐
│  1. Бот запускается и начинает цикл     │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  2. Каждые 30 секунд проверяет новые    │
│     сообщения в мир-чате                │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  3. Если находит сообщение от           │
│     пользователя с ключевыми словами    │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  4. Выбирает случайного персонажа       │
│     и отправляет подходящий ответ       │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│  5. Каждые 2-10 минут может отправить   │
│     случайное сообщение (30% шанс)      │
└─────────────────────────────────────────┘
```

## Логи

Бот выводит информацию о своей работе:

```
2024-01-01 12:00:00 - INFO - 🤖 Бот активности запущен!
2024-01-01 12:00:00 - INFO - 📊 Количество персонажей: 5
2024-01-01 12:00:00 - INFO - ⏱️ Интервал проверки: 30с
2024-01-01 12:00:00 - INFO - 💬 Интервал сообщений: 120-600с
2024-01-01 12:00:30 - INFO - 📩 Новое сообщение от user123: Привет всем!
2024-01-01 12:00:35 - INFO - ✅ Отправлено: [bot_maria] Привет! 👋 Как дела?
```

## Остановка

Нажмите **Ctrl+C** для остановки бота.

## Интеграция с фронтендом

Для отображения мир-чата в веб-приложении создайте компонент:

```jsx
// components/WorldChat.tsx
import { useEffect, useState } from 'react';

export default function WorldChat() {
  const [messages, setMessages] = useState([]);
  const [newMessage, setNewMessage] = useState('');

  useEffect(() => {
    // Загрузка сообщений
    fetch('/api/world-chat?limit=50')
      .then(res => res.json())
      .then(data => setMessages(data.messages));
    
    // Обновление каждые 5 секунд
    const interval = setInterval(() => {
      fetch('/api/world-chat?limit=50')
        .then(res => res.json())
        .then(data => setMessages(data.messages));
    }, 5000);
    
    return () => clearInterval(interval);
  }, []);

  const sendMessage = async () => {
    if (!newMessage.trim()) return;
    
    await fetch('/api/world-chat', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        userId: 'user_' + Date.now(),
        message: newMessage
      })
    });
    
    setNewMessage('');
  };

  return (
    <div className="world-chat">
      <div className="messages">
        {messages.map(msg => (
          <div key={msg.id} className={msg.isBot ? 'bot' : 'user'}>
            <strong>{msg.isBot ? '🤖' : '👤'}</strong>
            <span>{msg.message}</span>
          </div>
        ))}
      </div>
      
      <div className="input">
        <input 
          value={newMessage}
          onChange={(e) => setNewMessage(e.target.value)}
          placeholder="Написать в общий чат..."
        />
        <button onClick={sendMessage}>Отправить</button>
      </div>
    </div>
  );
}
```

## Безопасность

⚠️ **Важно:**

1. Не используйте production токены в коде
2. Ограничьте частоту сообщений
3. Добавьте модерацию контента
4. Настройте rate limiting в API

## Рекомендации

💡 **Лучшие практики:**

- Запускайте бота на сервере (не локально)
- Используйте PM2 или systemd для автозапуска
- Настройте логирование в файл
- Мониторьте работу бота
- Периодически обновляйте фразы персонажей

## Деплой на сервер

### Использование PM2 (рекомендуется)

```bash
# Установка PM2
npm install -g pm2

# Запуск бота
pm2 start chat_activity_bot.py --name "activity-bot" --interpreter python3

# Автозапуск при перезагрузке
pm2 startup
pm2 save

# Просмотр логов
pm2 logs activity-bot

# Перезапуск
pm2 restart activity-bot
```

### Использование systemd (Linux)

Создайте файл `/etc/systemd/system/activity-bot.service`:

```ini
[Unit]
Description=Chat Activity Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/anon-board-bot
ExecStart=/usr/bin/python3 chat_activity_bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:

```bash
sudo systemctl daemon-reload
sudo systemctl enable activity-bot
sudo systemctl start activity-bot
sudo systemctl status activity-bot
```

## Поддержка

Если возникли проблемы:

1. Проверьте логи бота
2. Убедитесь что API работает
3. Проверьте подключение к базе данных
4. Проверьте .env файл

## Лицензия

MIT
